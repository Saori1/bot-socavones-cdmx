services:
  - type: web
    name: bot-socavones-cdmx
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot_alertas_socavones.py
    envVars:
      - key: BOT_TOKEN
        value: your_telegram_bot_token_here
